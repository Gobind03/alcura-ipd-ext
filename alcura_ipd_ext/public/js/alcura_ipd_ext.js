/**
 * Alcura IPD Extensions – Desk bundle
 *
 * This file is loaded on every Desk page when `app_include_js` is enabled
 * in hooks.py.  Keep it lightweight; heavy per-DocType logic should live in
 * dedicated doctype_js files instead.
 */

$(document).ready(function () {
	// Uncomment to verify the bundle loads:
	// console.log("alcura_ipd_ext desk JS loaded");
});
